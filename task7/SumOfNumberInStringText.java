package tests.task7;

import java.util.Arrays;
import java.util.List;
import java.util.stream.IntStream;
/*
//В заданному тексте найти сумму всех встречающихся цифр
public class SumOfNumberInStringText {
    public static void main(String[] args) {
        String sourceArray = "887i9qwerty1qwerty24"; //исходный массив

        sourceArray = sourceArray.replaceAll("[^0-9]+", " ");

        int[] arr = Arrays.stream(sourceArray.substring().split(","))
                .map(String::trim).mapToInt(Integer::parseInt).toArray();
        System.out.println(Arrays.toString(arr));

        System.out.println("sum of numbers: ");
        int sum = 0;
        for(int i =0; i < arr.length(); i++) {
                sum += i;
        }
        System.out.println("sum = " + sum);
    }
}
*/